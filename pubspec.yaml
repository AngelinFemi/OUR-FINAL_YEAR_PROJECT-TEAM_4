name: food_donation_app
description: A Food Donation App for Donors and Volunteers
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^4.2.1
  latlong2: ^0.9.1
  location: ^5.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
